__Минимум__ (точно надо сделать)
- Контроллер для RGB лент 12 (возможно 5) вольт на Atmega328 (камень с ардуино)
-- цвет задается с базы, передается по радио
- База на wemos, на нем поднята локальная страничка, с которой можно будет настраивать цвета лент на (1). Одна база поддерживает несколько контроллеров
__Желательно__ (будет прикольно)
- ЛГБТ контроллер для адресных лент с эффектами (перелив радугой итд) / пользовательскими рисунками (каждый диод светится своим цветом)
-- тк адресные ленты управляются по Data-линии, задавать им цвет легче, но это требует больше вычислительных затрат. Это означает, что для управления большим кол-вом диодов может понадобиться оверклок
- Контроллер для мощных лент для основного освещения на 24 вольт
-- Легко сделать, но времени на него не останется)
__Дополнения__ (с этим проект будет выглядеть серьезнее)
- Перевести все устройства на PCB (печатные платы)
-- это легко, но требуется время на доставку из Китая
- Подвязать все к Алисе
-- Это, возможно, самая сложная задача, но если получится будет прям классно
- Добавить эффектов (стобоскоп, дыхание, итд)
-- Делается быстро, точно надо делать, если время будет позволять
